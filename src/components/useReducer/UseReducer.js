import React from "react";
import Count from "./Count";

const UseReducer = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Count />
    </div>
  );
};

export default UseReducer;
